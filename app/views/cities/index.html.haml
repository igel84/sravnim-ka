%legend Выберите город
%table{style: 'margin: 0 auto;'}
  - City.all.each_with_index do |city, index|
    != '<tr>' if (index+1)%3 == 1
    %td
      = link_to city.alias, root_url(subdomain: city.name), class: 'btn btn-info btn-large', style: 'width:150px'
    != '</tr>' if (index+1)%3 == 0