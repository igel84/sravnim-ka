%legend Выберите город
%table{style: 'margin: 0 auto;'}
  - City.all.each_with_index do |city, index|
    != "<tr>" if (index+1)%3 == 1
    !=  "<td>" + (link_to city.name, {controller: 'cities', action: 'show', id: city.id}, class: 'btn btn-info btn-large', style: 'width:150px') + "</td>"
    != "</tr>" if (index+1)%3 == 0